@model IEnumerable<PruebaTécnica.Modelo.USERROL>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Principal</h2>

<p>
    @Html.ActionLink("Crear Nueva Asignación", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Nombre del Rol")
        </th>
        <th>
            @Html.DisplayName("Nombre del Usuario")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ROLES.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.USUARIOS.Nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.ID }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
